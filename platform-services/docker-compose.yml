version: '3'
services:
    platform-services:
        image: elastest/platform-services:0.5.0-alpha1
        volumes:
            - platform-services:/platform-services
        networks:
            - elastest
networks:
    elastest:
      driver: bridge
volumes:
    platform-services:
      driver: local
